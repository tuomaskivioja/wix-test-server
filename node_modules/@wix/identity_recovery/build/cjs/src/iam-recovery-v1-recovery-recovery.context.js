"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.recover = exports.sendActivationEmail = exports.sendRecoveryEmail = void 0;
const iam_recovery_v1_recovery_recovery_public_1 = require("./iam-recovery-v1-recovery-recovery.public");
const sendRecoveryEmail = (...args) => {
    const context = 
    // @ts-expect-error
    typeof $wixContext !== 'undefined'
        ? // @ts-expect-error
            $wixContext
        : // @ts-expect-error
            globalThis.__wix_context__;
    if (!context) {
        throw new Error('Wix context is not available. Make sure to initialize the Wix context before using SDK modules');
    }
    return context
        .initWixModules(iam_recovery_v1_recovery_recovery_public_1.sendRecoveryEmail, args[1]?.supressAuth ? true : false)
        .apply(undefined, args);
};
exports.sendRecoveryEmail = sendRecoveryEmail;
const sendActivationEmail = (...args) => {
    const context = 
    // @ts-expect-error
    typeof $wixContext !== 'undefined'
        ? // @ts-expect-error
            $wixContext
        : // @ts-expect-error
            globalThis.__wix_context__;
    if (!context) {
        throw new Error('Wix context is not available. Make sure to initialize the Wix context before using SDK modules');
    }
    return context
        .initWixModules(iam_recovery_v1_recovery_recovery_public_1.sendActivationEmail, args[1]?.supressAuth ? true : false)
        .apply(undefined, args);
};
exports.sendActivationEmail = sendActivationEmail;
const recover = (...args) => {
    const context = 
    // @ts-expect-error
    typeof $wixContext !== 'undefined'
        ? // @ts-expect-error
            $wixContext
        : // @ts-expect-error
            globalThis.__wix_context__;
    if (!context) {
        throw new Error('Wix context is not available. Make sure to initialize the Wix context before using SDK modules');
    }
    return context
        .initWixModules(iam_recovery_v1_recovery_recovery_public_1.recover, args[1]?.supressAuth ? true : false)
        .apply(undefined, args);
};
exports.recover = recover;
