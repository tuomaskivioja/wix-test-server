import { sendRecoveryEmail as publicSendRecoveryEmail, sendActivationEmail as publicSendActivationEmail, recover as publicRecover, } from './iam-recovery-v1-recovery-recovery.public';
export const sendRecoveryEmail = (...args) => {
    const context = 
    // @ts-expect-error
    typeof $wixContext !== 'undefined'
        ? // @ts-expect-error
            $wixContext
        : // @ts-expect-error
            globalThis.__wix_context__;
    if (!context) {
        throw new Error('Wix context is not available. Make sure to initialize the Wix context before using SDK modules');
    }
    return context
        .initWixModules(publicSendRecoveryEmail, args[1]?.supressAuth ? true : false)
        .apply(undefined, args);
};
export const sendActivationEmail = (...args) => {
    const context = 
    // @ts-expect-error
    typeof $wixContext !== 'undefined'
        ? // @ts-expect-error
            $wixContext
        : // @ts-expect-error
            globalThis.__wix_context__;
    if (!context) {
        throw new Error('Wix context is not available. Make sure to initialize the Wix context before using SDK modules');
    }
    return context
        .initWixModules(publicSendActivationEmail, args[1]?.supressAuth ? true : false)
        .apply(undefined, args);
};
export const recover = (...args) => {
    const context = 
    // @ts-expect-error
    typeof $wixContext !== 'undefined'
        ? // @ts-expect-error
            $wixContext
        : // @ts-expect-error
            globalThis.__wix_context__;
    if (!context) {
        throw new Error('Wix context is not available. Make sure to initialize the Wix context before using SDK modules');
    }
    return context
        .initWixModules(publicRecover, args[1]?.supressAuth ? true : false)
        .apply(undefined, args);
};
//# sourceMappingURL=iam-recovery-v1-recovery-recovery.context.js.map