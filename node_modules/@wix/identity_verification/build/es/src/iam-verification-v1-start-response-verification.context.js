import { start as publicStart, verifyDuringAuthentication as publicVerifyDuringAuthentication, resendDuringAuthentication as publicResendDuringAuthentication, } from './iam-verification-v1-start-response-verification.public';
export const start = (...args) => {
    const context = 
    // @ts-expect-error
    typeof $wixContext !== 'undefined'
        ? // @ts-expect-error
            $wixContext
        : // @ts-expect-error
            globalThis.__wix_context__;
    if (!context) {
        throw new Error('Wix context is not available. Make sure to initialize the Wix context before using SDK modules');
    }
    return context
        .initWixModules(publicStart, args[1]?.supressAuth ? true : false)
        .apply(undefined, args);
};
export const verifyDuringAuthentication = (...args) => {
    const context = 
    // @ts-expect-error
    typeof $wixContext !== 'undefined'
        ? // @ts-expect-error
            $wixContext
        : // @ts-expect-error
            globalThis.__wix_context__;
    if (!context) {
        throw new Error('Wix context is not available. Make sure to initialize the Wix context before using SDK modules');
    }
    return context
        .initWixModules(publicVerifyDuringAuthentication, args[1]?.supressAuth ? true : false)
        .apply(undefined, args);
};
export const resendDuringAuthentication = (...args) => {
    const context = 
    // @ts-expect-error
    typeof $wixContext !== 'undefined'
        ? // @ts-expect-error
            $wixContext
        : // @ts-expect-error
            globalThis.__wix_context__;
    if (!context) {
        throw new Error('Wix context is not available. Make sure to initialize the Wix context before using SDK modules');
    }
    return context
        .initWixModules(publicResendDuringAuthentication, args[1]?.supressAuth ? true : false)
        .apply(undefined, args);
};
//# sourceMappingURL=iam-verification-v1-start-response-verification.context.js.map