import { token as publicToken } from './identity-oauth-v1-refresh-token-oauth.public';
export const token = (...args) => {
    const context = 
    // @ts-expect-error
    typeof $wixContext !== 'undefined'
        ? // @ts-expect-error
            $wixContext
        : // @ts-expect-error
            globalThis.__wix_context__;
    if (!context) {
        throw new Error('Wix context is not available. Make sure to initialize the Wix context before using SDK modules');
    }
    return context
        .initWixModules(publicToken, args[1]?.supressAuth ? true : false)
        .apply(undefined, args);
};
//# sourceMappingURL=identity-oauth-v1-refresh-token-oauth.context.js.map